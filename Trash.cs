
//for (int i = 0; i < startVertices.Length; i++)
//{
//    toVertices[i].y = startVertices[i].y + value;
//}
//Debug.Log(toVertices[0].y);
////set things back
//mesh.vertices = toVertices;
//textMesh.canvasRenderer.SetMesh(mesh);
